{
  "name": "@neurocache/api",
  "version": "0.1.0",
  "private": true,
  "description": "NeuroCache engine — Go backend (invoked via Turborepo)",
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "go build -o bin/server ./cmd/server && go build -o bin/cli ./cmd/cli",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "clean": "rm -rf bin data"
  }
}
